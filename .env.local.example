# Copy to .env.local and fill in from your Supabase project settings
# (Project Settings → API). Until these are set, the app runs in demo
# mode: no auth, seed data from lib/mockGoals.ts, nothing persists.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
